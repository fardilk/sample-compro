# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

gh_ci2.b64
github_ci.b64

# Secrets — never commit.
# .env.production stays tracked on purpose: it holds only VITE_SITE_URL,
# which the CI build needs to substitute into index.html.
.env
.env.local
.env.development
.env.*.local
*.pem
*.key
*.b64

# Astro generated types
.astro
